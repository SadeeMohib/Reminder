<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reminderapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Medexox"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApp">
        <activity android:name=".MyAccount"></activity>
        <activity android:name=".ForgotPassWordActivity" />
        <activity android:name=".DashBoard" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DataBaseActivity" />
        <activity android:name=".Medication" />
        <activity android:name=".Appoinment" />
        <activity android:name=".AddMedication" />
        <activity android:name=".AddAppoinment" />
        <activity
            android:name=".ui.login.DataBaseActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>